# ============================================================================
# FICHIER env.example - DiaspoMoney
# ============================================================================
# Copiez ce fichier vers .env.dev, .env.rct ou .env.prod selon l'environnement
# cp env.example .env.dev
# ============================================================================

# === ENVIRONNEMENT ===
# dev | recette | production
NODE_ENV=development
LOG_LEVEL=debug

# === MONGODB ===
MONGO_PASSWORD=change-me-strong-password-123
MONGO_ADMINUSERNAME=admin
MONGO_USER=admin
MONGO_HOST=mongodb
MONGO_INITDB_ROOT_USERNAME=${MONGO_ADMINUSERNAME}
MONGO_INITDB_ROOT_PASSWORD=${MONGO_PASSWORD}
# URI MongoDB (sera construite automatiquement si non définie)
MONGODB_URI=mongodb://${MONGO_ADMINUSERNAME}:${MONGO_PASSWORD}@${MONGO_HOST}:27017/diaspomoney?authSource=${MONGO_ADMINUSERNAME}
MONGO_URI=${MONGODB_URI}

# === REDIS ===
REDIS_PASSWORD=${MONGO_PASSWORD}
REDIS_URL=redis://${MONGO_ADMINUSERNAME}:${REDIS_PASSWORD}@redis:6379

# === JWT & SÉCURITÉ ===
# Générez des secrets forts avec: openssl rand -hex 64
JWT_SECRET=change-me-jwt-secret-key-min-64-chars
JWT_REFRESH_SECRET=change-me-refresh-secret-key-min-64-chars
AUTH_SECRET=change-me-auth-secret-base64
NEXTAUTH_SECRET=${AUTH_SECRET}
ENCRYPTION_MASTER_KEY=change-me-encryption-key-min-64-chars
ENCRYPTION_KEY=${ENCRYPTION_MASTER_KEY}
SESSION_SECRET=${AUTH_SECRET}

# === ADMIN ===
ADMIN_AUTH=change-me-admin-password

# === URLs ===
# IMPORTANT: Configurez ces variables selon votre environnement
# 
# Pour DEV:
#   APP_URL=https://dev.diaspomoney.fr
#   API_URL=https://dev.diaspomoney.fr/api
#   NEXTAUTH_URL=https://dev.diaspomoney.fr
#   NEXT_PUBLIC_APP_URL=https://dev.diaspomoney.fr
#   NEXT_PUBLIC_API_URL=https://dev.diaspomoney.fr/api
#
# Pour RCT (Recette):
#   APP_URL=https://rct.diaspomoney.fr
#   API_URL=https://rct.diaspomoney.fr/api
#   NEXTAUTH_URL=https://rct.diaspomoney.fr
#   NEXT_PUBLIC_APP_URL=https://rct.diaspomoney.fr
#   NEXT_PUBLIC_API_URL=https://rct.diaspomoney.fr/api
#
# Pour PROD (Production):
#   APP_URL=https://app.diaspomoney.fr
#   API_URL=https://app.diaspomoney.fr/api
#   NEXTAUTH_URL=https://app.diaspomoney.fr
#   NEXT_PUBLIC_APP_URL=https://app.diaspomoney.fr
#   NEXT_PUBLIC_API_URL=https://app.diaspomoney.fr/api
APP_URL=https://app.diaspomoney.fr
API_URL=https://app.diaspomoney.fr/api
URL=${APP_URL}
URL_INTERNAL=http://localhost:3000
NEXTAUTH_URL=${APP_URL}
NEXT_PUBLIC_APP_URL=${APP_URL}
NEXT_PUBLIC_API_URL=${API_URL}

# === GOOGLE OAUTH ===
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# === FACEBOOK OAUTH ===
FACEBOOK_CLIENT_ID=your-facebook-app-id
FACEBOOK_CLIENT_SECRET=your-facebook-app-secret

# === EMAIL (Resend) ===
RESEND_API_KEY=re_your-resend-api-key

# === STRIPE ===
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key
STRIPE_WEBHOOK_SECRET=whsec_your-stripe-webhook-secret
STRIPE_PUBLIC_KEY=pk_live_your-stripe-public-key

# === PAYPAL ===
PAYPAL_CLIENT_ID=your-paypal-client-id
PAYPAL_CLIENT_SECRET=your-paypal-client-secret

# === MONITORING ===
SENTRY_DSN=your-sentry-dsn-url
GRAFANA_PASSWORD=${MONGO_PASSWORD}

# === BACKUPS ===
BACKUPS_DIR=./backups

# === SMTP (Optionnel - si vous n'utilisez pas Resend) ===
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASSWORD=password
SMTP_FROM=noreply@diaspomoney.fr

# === MONGO EXPRESS (Optionnel - pour développement) ===
ME_CONFIG_MONGODB_SERVER=mongodb
ME_CONFIG_MONGODB_PORT=27017
ME_CONFIG_MONGODB_ADMINUSERNAME=${MONGO_ADMINUSERNAME}
ME_CONFIG_MONGODB_AUTH_DATABASE=${MONGO_ADMINUSERNAME}
ME_CONFIG_BASICAUTH_USERNAME=${MONGO_ADMINUSERNAME}
ME_CONFIG_BASICAUTH_PASSWORD=${MONGO_PASSWORD}
ME_CONFIG_MONGODB_ADMINPASSWORD=${MONGO_PASSWORD}
ME_CONFIG_MONGODB_URL=mongodb://${MONGO_ADMINUSERNAME}:${MONGO_PASSWORD}@${MONGO_HOST}:27017/

# === NEXT.JS ===
NEXT_TELEMETRY_DISABLED=1

